import 'package:flutter/material.dart';

class MediaTabContent extends StatelessWidget {
  const MediaTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('MEDIA TAB')));
  }
}
